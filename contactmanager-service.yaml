apiVersion: v1
kind: Service
metadata:
  name: contactmanager-service  # Service Name
spec:
  selector:
    app: contactmanager  
  ports:
    - protocol: "TCP"
      port: 8080
      targetPort: 8080
  type: NodePort
